import React from "react";
import { createRoot } from "react-dom/client";
import Landed from "./landed-tax-router.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Landed />
  </React.StrictMode>
);
